'use client';
import {useState} from 'react';
export default function AngelLogin(){const [p,setP]=useState('');const [err,setErr]=useState('');const submit=async(e:React.FormEvent)=>{e.preventDefault();const r=await fetch('/api/angel/login',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({password:p})});if(r.ok){location.href='/angel'}else setErr('ورود نامعتبر')};return <main><div className="container login-wrap"><form className="card login-card" onSubmit={submit}><span className="eyebrow"><span className="dot"/> PRIVATE · OWNER ONLY</span><h2>Horizon Angel</h2><p className="muted small">کنسول خصوصی مالک سیستم. کلید خصوصی لایسنس در این UI ذخیره نمی‌شود.</p><div className="field"><label>Owner password</label><input type="password" value={p} onChange={e=>setP(e.target.value)} required/></div>{err&&<div className="notice danger-text">{err}</div>}<button className="btn btn-primary" style={{width:'100%',marginTop:12}}>ورود امن</button></form></div></main>}
